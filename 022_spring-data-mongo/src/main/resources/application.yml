spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: huseyin_mongodb

logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG